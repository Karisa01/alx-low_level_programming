#include "main.h"
#include <stdio.h>
void swap_int(int *a, int *b);
{
int a, b;
printf("Enter Value of a ");
scanf("%d", &a);
printf("\nEnter Value of b ");
scanf("%d", &b);
int temp = a;
a = b;
b = temp;
printf("\nAfter Swapping: a = %d, b = %d", x, y);
return 0;
}
